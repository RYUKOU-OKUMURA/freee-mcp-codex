# AGENTS.md

このリポジトリで Codex が作業するときの安全ルールです。

目的

- このリポジトリは freee-mcp を Codex デスクトップアプリで安全に使うための非公式スターターキットを整えることを目的にする。
- freee 本番データや顧問先データを危険に扱わないことを最優先にする。
- freee 公式、freee 公認、freee 認定と誤認される表現は使わない。

基本ルール

- まず読み取り系の確認を優先する。
- いきなり登録、更新、削除をしない。
- 不明点は断定せず、要確認として分ける。
- 顧問先データを勝手に横断しない。
- 認証情報、Client Secret、アクセストークン、リフレッシュトークン、秘密鍵、設定ファイルを Git 管理しない。
- freee の税務判断、会計判断、削除判断を AI の最終結論にしない。

freee 操作ルール

1. 取得、検索、一覧表示などの読み取り操作を最優先する。
2. 登録、更新、削除は、必ず事前確認を取る。
3. 取引、請求書、経費精算の作成は、まず下書き案を表で出す。
4. 実行前に、API パス、HTTP メソッド、送信内容、影響範囲を日本語で説明する。
5. 税区分、勘定科目、取引先、金額、日付は必ず確認項目に含める。
6. 判断に迷うものは要確認として分類する。
7. 顧問先データを勝手に横断しない。
8. 削除系 API は原則使わない。
9. 本番データ操作は、検証完了後に限定する。
10. AI の判断を税務判断の最終結論にしない。
11. Client Secret、アクセストークン、リフレッシュトークン、認証設定ファイルを Git 管理しない。
12. 実行後は、何を取得、作成、更新したかを日本語で要約する。

実行時の説明ルール

- API を実行する前に、何をするかを日本語で説明する。
- 実行後に、結果と次に人間が確認すべき点を日本語で説明する。
- 曖昧な項目は、推測で埋めずに要確認と書く。

禁止事項

- ユーザーが明示的に許可しない限り、POST、PUT、PATCH、DELETE を実行しない。
- 削除を自動化しない。
- 顧問先をまたいで情報を混ぜない。
- 秘密情報をプロンプトやログに不用意に貼らない。
- 公式提供物であるかのような説明をしない。

判断の基準

- 読み取りで足りるなら、そこで止める。
- 書き込みが必要なら、下書きと影響範囲の説明を先に出す。
- 人間確認が必要なら、実行しない。
